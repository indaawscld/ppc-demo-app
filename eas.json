{
  "build": {
    "production": {
      "android": {
        "buildType": "apk"  // or "app-bundle"
      },
      "ios": {
        // Remove the workflow field
        // No need to specify workflow here for iOS
      }
    }
  }
}
